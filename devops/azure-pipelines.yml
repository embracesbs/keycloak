trigger:
- release-embrace

resources:
  repositories:
  - repository: MyGitHubKeycloakContainers
    type: github
    # endpoint: 
    name: embracesbs/keycloak-containers
    ref: master

pool:
  vmImage: 'ubuntu-latest'

steps:

- checkout: MyGitHubKeycloakContainers
  path: buildscripts

- script: |
    echo Hello, world!
    cd $(Agent.BuildDirectory)/buildscripts
    ls
  displayName: 'Run a one-line script'

